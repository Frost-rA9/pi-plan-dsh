{
  "name": "pi-plan-dsh-core",
  "version": "0.1.0",
  "type": "module",
  "description": "pi-plan-dsh core (host): /plan command, plan/mode log fold, plan:policy section injection, exit_plan_mode approval tool. Single reference source: dsh plan-mode.",
  "keywords": [
    "pi-package",
    "pi-extension"
  ],
  "license": "MIT",
  "pi": {
    "extensions": ["./index.ts"]
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --experimental-strip-types test/load.spec.ts"
  },
  "dependencies": {
    "pi-plan-dsh-bridge": "file:../bridge"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*",
    "typebox": "*"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": ">=0.84.4",
    "@earendil-works/pi-tui": ">=0.84.4",
    "@types/node": "^24.0.0",
    "typebox": "^1.0.0",
    "typescript": "^5.9.3"
  }
}
