{
  "name": "pi-plan-dsh-bridge",
  "version": "0.1.0",
  "type": "module",
  "description": "Shared contract for pi-plan-dsh: plan/mode log fold, plan:policy render, config validation, markdown heading checks. Single reference source: dsh plan-mode.",
  "keywords": [
    "pi-package",
    "pi-plan"
  ],
  "license": "MIT",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --experimental-strip-types test/bridge.spec.ts"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": ">=0.84.4",
    "@types/node": "^24.0.0",
    "typescript": "^5.9.3"
  }
}
